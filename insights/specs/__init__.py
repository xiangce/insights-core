from insights.core.spec_factory import SpecSet, RegistryPoint


class Specs(SpecSet):
    pass
    # Client metadata specs/files
    # redhat_release = RegistryPoint(no_obfuscate=['hostname', 'ipv4', 'ipv6', 'mac'])
    # uname = RegistryPoint(no_obfuscate=['ipv4', 'ipv6', 'mac'])
